python-dotenv>=1.0.0
aiohttp>=3.10.0
PyYAML>=6.0
qrcode>=8.0
pygdbmi>=0.11
pypdf>=5.4.0
tzdata>=2025.2
ag-ui-protocol==0.1.19

# Windows PTY support for Web terminal
pywinpty>=2.0.15; platform_system == "Windows"

# Process management (optional, for better /exec timeout handling)
psutil>=5.9.0

# Test dependencies (optional)
pytest>=7.0
pytest-asyncio>=0.21.0
