{
  "schemaVersion": 1,
  "actions": [
    {
      "id": "git.status",
      "label": "Git 状态",
      "icon": "GitBranch",
      "windowsCommand": "git status --short",
      "linuxCommand": "git status --short",
      "macosCommand": "git status --short",
      "cwd": ".",
      "confirm": false,
      "enabled": true
    },
    {
      "id": "test.backend",
      "label": "后端测试",
      "icon": "TestTube2",
      "windowsCommand": "python -m pytest tests -q",
      "linuxCommand": "python -m pytest tests -q",
      "macosCommand": "python -m pytest tests -q",
      "cwd": ".",
      "confirm": false,
      "enabled": true
    },
    {
      "id": "test.front",
      "label": "前端门禁",
      "icon": "TestTube2",
      "windowsCommand": "npm run test:gate",
      "linuxCommand": "npm run test:gate",
      "macosCommand": "npm run test:gate",
      "cwd": "front",
      "confirm": false,
      "enabled": true
    }
  ]
}
